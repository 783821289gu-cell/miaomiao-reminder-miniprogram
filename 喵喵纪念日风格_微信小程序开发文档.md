# 喵喵记日微信小程序开发文档

## 1. 当前目标

喵喵记日是一个用于记录重要日、待办和生活小事的微信小程序。事项以微信云数据库为正式数据源，本机 storage 仅作弱网缓存；语音转文字采用阿里云百炼 Paraformer 实时语音识别。

首版只支持新增事项。语音识别得到的文字、文字导入内容、手动创建表单，最终都只能生成新事件；删除、修改、查询等意图都给出提醒，不自动处理。

## 2. 首页与交互

- 首页显示猫咪主题背景、顶部品牌区、背景切换、分类筛选、事件卡片列表、底部导入按钮、长按说话按钮和居中的猫爪添加按钮。
- 事件卡片显示标题、日期、倒数状态、分类和菜单入口。
- 今天事项按最高优先级置顶，并使用更醒目的暖色卡片底色标识。
- 当天事项中间显示 `就是今天`，不显示 `0 天`。
- 明天事项状态显示 `明天`，后天显示 `后天`，更远未来显示 `未来`，过期事项显示 `已过`。
- 点击事项时，中间弹出符合猫咪主题风格的大号提示气泡，约 1.4 秒后自动消失。
- 所有业务弹窗支持点击遮罩关闭。底部弹窗顶部保留短横条，向下拖拽关闭，向上只允许轻微弹性位移。
- 输入事件名称 `959499` 时不保存事件，只弹出透明抱心猫轮廓动画，约 1.35 秒后消失。

## 3. 新增事件入口

### 手动创建

- 表单字段：事项名称、日期、时间、分类、背景。
- 日期选择器保持猫咪主题风格，只保留公历三列滚轮和取消/确定按钮。
- 不提供重复频率功能，所有首版事件默认不重复。
- 时间输入支持自动格式化，例如 `8点17`、`8.17`、`八点十七` 都保存为 `08:17`。

### 语音记事

- 语音入口不是可持续打开的页面功能，只在手指长按按钮时录音，手指离开按钮后停止录音并开始解析。
- 未开启麦克风权限时只提示权限问题，不展示识别文本或确认表单。
- 语音识别使用阿里云百炼 DashScope WebSocket 接口和 `paraformer-realtime-v2` 模型，前端录制 16k、16bit、单声道 PCM 并实时发送；启用低延迟 VAD 断句、标点预测和中文数字格式化。
- 永久 `DASHSCOPE_API_KEY` 只配置在 `getDashScopeToken` 云函数环境变量中；前端只使用短期凭证。小程序后台需要把 `wss://dashscope.aliyuncs.com` 加入 socket 合法域名。
- 语音文字只按新增事件解析。无日期、无事项、修改/删除/查询类内容都提示示例，不生成记录。
- 语音确认弹窗只保留 `取消` 和 `生成记录` 两个按钮；用户需要重说时关闭弹窗后重新长按。

### 文字导入

- 导入弹层标题为 `文字导入`。
- 只提供多行文本输入框和解析按钮，不提供图片选择。
- placeholder 使用不敏感示例：
  - `明天交作业`
  - `下周五团队会议`
  - `8月1日朋友生日`
- 多行文本按换行、分号拆分。有效行进入确认列表；无效行显示原因，不能保存。

## 4. 文字解析规则

统一入口为 `parseCreateEventText(rawText, now)`，语音和文字导入共用。

可识别日期：

- 相对日期：`今天`、`明天`、`后天`、`大后天`
- 周日期：`本周五`、`这周五`、`周五`、`下周五`
- 月日期：`下个月10号`、`8月1日`
- 完整日期：`2026年8月1日`、`2026-08-01`、`8/1`

可识别时间：

- `早上八点`
- `下午3点半`
- `18:30`
- `8：17`
- `8.17`
- `8点17`
- `八点十七`
- `八点半`

不生成事项的情况：

- 空文本、纯闲聊、无意义内容。
- 有日期但没有事项，例如 `明天`。
- 有事项但没有日期，例如 `吃饭`。
- 修改、删除、查询类表达，例如 `删除明天的会议`、`把会议改到后天`、`查一下明天有什么`。

失败提醒使用友好示例，例如 `试试：明天交作业`、`需要带上日期，例如明天交作业`、`当前只支持新增事项`。

## 5. 数据结构

事件保存在本地 storage，核心字段：

```js
{
  id: string,
  title: string,
  targetDate: string,
  targetTime: string,
  location: string,
  categoryId: string,
  repeat: string,
  reminderOffsets: string[],
  backgroundId: string,
  sourceType: "manual" | "voice" | "text_import",
  sourceText: string,
  createdAt: number,
  updatedAt: number
}
```

## 6. 验收标准

- 首页无外部服务报错，微信开发者工具预览正常。
- 真机长按语音按钮时启动录音，松手后听写浮层立即消失，并通过阿里云 Paraformer 返回真实识别文本。
- 未授权麦克风、未配置阿里云百炼密钥或 socket 域名未配置时只提示原因，不展示固定示例识别结果。
- 搜索不到云端、图片识别、外部模型相关调用。
- 当天事件不显示 `0 天`。
- 当天事件置顶，并使用今日专属底色。
- 添加事件中不出现重复频率字段。
- `这周三事项` 在 2026-06-25 解析为 2026-06-24，`下周三事项` 解析为 2026-07-01。
- `明天8.17交作业`、`明天八点十七交作业` 能生成 `08:17` 的时间。
- `明天交作业`、`下周五团队会议`、`8月1日朋友生日` 能生成新增事项。
- `吃饭`、`明天`、`哈哈哈`、`删除明天的会议` 不生成事项，并出现清晰提醒。
- 导入多行文本时，有效候选可保存，无效候选不能保存。
